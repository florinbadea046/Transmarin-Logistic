// ──────────────────────────────────────────────────────────
// Unit tests: Export utilities
// File: src/modules/accounting/invoices-export.ts
//
// Ce trebuie testat:
// - getExportRows: transforms invoice objects to flat export row format
// - exportPDF: calls jsPDF with correct table structure and data
// - exportExcel: calls XLSX utils to create workbook with named sheet
// - exportCSV: generates valid CSV string with headers and escaped values
// ──────────────────────────────────────────────────────────

import { describe, it } from "vitest";

describe("invoices-export", () => {
  it.todo("placeholder — implementeaza testele");
});
